// src/ui/locations.js
// Управление локациями (точками продаж)

export function renderLocSwitcherUI() { return window.renderLocSwitcherUI(); }
export function renderLocList()       { return window.renderLocList(); }
export function toggleLocMenu(e)      { return window.toggleLocMenu(e); }
export function toggleExportMenu(e)   { return window.toggleExportMenu(e); }
export function switchLocation(id)    { return window.switchLocation(id); }
export function openAddLocation()     { return window.openAddLocation(); }
export function renameActiveLocation(){ return window.renameActiveLocation(); }
export function deleteActiveLocation(){ return window.deleteActiveLocation(); }
export function saveLocation()        { return window.saveLocation(); }
